---

# -------------------------------------------------------------------------------------
# Golang
# -------------------------------------------------------------------------------------
- name: go
  hosts: nodes 
  become: true
  become_user: root
  become_method: sudo  
  vars: 
    user: "{{ user }}"
    version: "{{ version }}"
  roles:
    - role: go