---

# -------------------------------------------------------------------------------------
# Projector
# -------------------------------------------------------------------------------------
- name: projector
  hosts: nodes 
  become: true
  become_user: root
  become_method: sudo  
  vars: 
    ide: "{{ ide }}"
    username: "debian"
  roles:
    - role: projector